# 🏥 Virtum Health App

Комплексное решение для мониторинга здоровья с использованием Flutter и Node.js. Включает мобильное приложение, серверную часть с ИИ-чатом (DeepSeek) и систему локального кэширования данных.

---

## 📂 Структура проекта

*   **`/mobile`** — Мобильное приложение на Flutter.
*   **`/backend`** — Серверная часть на Node.js (Express).

---

## 🚀 Как запустить проект

### 1. Запуск Backend (Сервер)
1. Установите [Node.js](https://nodejs.org/).
2. Перейдите в папку backend:
   ```bash
   cd backend
   ```
3. Установите зависимости:
   ```bash
   npm install
   ```
4. Запустите сервер:
   ```bash
   npm start
   ```
   *Сервер будет запущен на порту 3000 и будет доступен по адресу `http://localhost:3000` и по вашему локальному IP.*

### 2. Запуск Mobile (Flutter)
1. Установите [Flutter SDK](https://docs.flutter.dev/get-started/install).
2. Перейдите в папку mobile:
   ```bash
   cd mobile
   ```
3. Получите зависимости:
   ```bash
   flutter pub get
   ```
4. Запустите приложение (убедитесь, что эмулятор или телефон подключены):
   ```bash
   flutter run
   ```

---

## 🌐 Настройка IP адреса (Для работы на реальном телефоне)

Чтобы приложение на телефоне могло "видеть" сервер на ноутбуке, они должны быть в одной Wi-Fi сети.

### Как найти свой IP:
1. В терминале Windows введите: `ipconfig`
2. Найдите строку `IPv4 Address` (обычно начинается на `192.168...`).

### Как поменять IP в коде:
1. Откройте файл: `mobile/lib/services/api_service.dart`.
2. Найдите строку:
   ```dart
   static const String baseUrl = 'http://192.168.0.12:3000';
   ```
3. Замените `192.168.0.12` на ваш актуальный IP.
4. После смены IP нужно **обязательно** пересобрать приложение или сделать Hot Restart.

---

## 🛠 Технологии

*   **Frontend**: Flutter, `fl_chart` (графики), `http` (API), `sqflite` (локальная БД).
*   **Backend**: Node.js, Express, DeepSeek API (ИИ чат).
*   **Database**: SQLite (локально на телефоне), In-memory storage (на сервере для тестов).

---

## 📝 Важные примечания для Android

*   **Разрешения**: В `AndroidManifest.xml` добавлено разрешение `android.permission.INTERNET`.
*   **HTTP**: Включен параметр `android:usesCleartextTraffic="true"` для возможности работы с локальным сервером без HTTPS.
*   **Сборка APK**: Для сборки используйте команду:
    ```bash
    flutter build apk --release
    ```

---

## 🤖 AI Чат (DeepSeek)
Сервер использует DeepSeek API для ответов на вопросы о здоровье. 
Ключ API находится в `backend/server.js`. В коде реализована система фильтрации ответов, чтобы ИИ давал полезные советы на основе ваших метрик.

---

## ⚖️ Лицензия
Проект создан в учебных целях.
